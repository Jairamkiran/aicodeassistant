<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logging configuration for the app.

  - Profile 'local' (and tests): human-readable console pattern including the
    correlationId + trace/span ids from the MDC.
  - Otherwise (containerised/prod): structured JSON via logstash-logback-encoder
    so Loki/promtail can parse fields (traceId, correlationId, level, logger).

  Trace/span ids are put on the MDC by Micrometer Tracing; correlationId by our
  CorrelationIdFilter. Emitting them in every line is what makes logs, metrics,
  and traces correlate in Grafana.
-->
<configuration>
  <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

  <springProfile name="local,test">
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
      <encoder>
        <pattern>%d{HH:mm:ss.SSS} %-5level [%X{correlationId:-}] [%X{traceId:-},%X{spanId:-}] %logger{36} - %msg%n</pattern>
      </encoder>
    </appender>
  </springProfile>

  <springProfile name="!local &amp; !test">
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
      <encoder class="net.logstash.logback.encoder.LogstashEncoder">
        <includeMdcKeyName>correlationId</includeMdcKeyName>
        <includeMdcKeyName>traceId</includeMdcKeyName>
        <includeMdcKeyName>spanId</includeMdcKeyName>
        <customFields>{"service":"aicodeassistant-app"}</customFields>
      </encoder>
    </appender>
  </springProfile>

  <root level="INFO">
    <appender-ref ref="STDOUT"/>
  </root>
</configuration>
