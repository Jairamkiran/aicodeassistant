/*
 * sdk bounded context (DDD module).
 *
 * Milestone status: STUB (M0). The build wiring, package root, and Modulith
 * module descriptor exist so the module compiles and participates in boundary
 * verification. Domain, application, and adapter code arrive in the milestone
 * that owns this context.
 */
plugins {
    id("aicodeassistant.spring-library-conventions")
}
